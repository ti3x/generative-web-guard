// Loaded by a script element that the (trusted, nonce'd) host module created
// with createElement + appendChild and NO nonce attribute. This is exactly the
// case 'strict-dynamic' exists to allow.
window.__result.tags.cdnProgrammaticInsert = "ran";
