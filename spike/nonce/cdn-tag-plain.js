// Parser-inserted CDN script WITHOUT a nonce. Under 'strict-dynamic' the
// host-source expression that would allow it is ignored, so whether this runs
// is the direct test of "is the <cdn> token in script-src still honoured".
window.__result.tags.cdnTagPlain = "ran";
